React Frontend

This folder will contain the React application that talks to the Django REST API.
Recommended options:

- Use Vite: `npm create vite@latest frontend --template react`
- Or Create React App: `npx create-react-app frontend`

After scaffold, implement auth using JWT and connect to the DRF endpoints.

